[package]
name = "vdoprf-offline"
version = "0.1.0"
edition = "2021"

[dependencies]
vdoprf-field = { path = "../field" }
vdoprf-ss = { path = "../secret-sharing" }
vdoprf-crypto = { path = "../crypto-primitives" }
vdoprf-network = { path = "../network" }
num-bigint = "0.4"
num-traits = "0.2"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
